/* tslint:disable:no-unused-variable */

describe('AppComponent', () => {

  beforeEach(() => {

  });

  fit('should contain the navigation bar component', () => {
      expect('true').toBe('true')
  });

});
